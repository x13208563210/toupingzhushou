直播投屏助手 安装版说明
版本：v{{APP_VERSION}}
日期：{{BUILD_DATE}}

安装后使用
1. 默认安装目录优先为 `{{DEFAULT_INSTALL_DIR}}`；如果对方电脑没有 D 盘，会自动改到系统默认程序目录。
2. 安装完成后可从桌面或开始菜单运行 `直播投屏助手`，也可以直接运行安装目录里的 `{{APP_EXE_NAME}}`。
3. 首次启动会弹出管理员权限确认，请点“是”，这样才能读取视频号直播伴侣的“互动消息”悬浮窗。
4. 如果弹出 Windows 防火墙提示，请允许局域网访问。

主要功能
1. 接收安卓投屏画面和声音。
2. 支持语音控制和语音点歌。
3. 支持弹幕提醒、礼物提醒、弹幕播报、自动回复。
4. 支持虚拟摄像头输出，方便 OBS 等软件采集。

默认配置说明
1. 如果打包这台电脑的 `{{DEFAULT_INSTALL_DIR}}` 下已经有自动回复配置和弹幕区域配置，安装包会一并带上，安装后默认直接可用。
2. 自动回复规则、礼物感谢文案、定时发送内容、弹幕区域会沿用打包时导入的默认配置。
3. 如果对方电脑上的视频号直播伴侣布局、缩放比例或窗口位置不同，仍然建议重新记录自动回复输入框和发送按钮位置。

注意事项
1. `voice-runtime`、`voice-model`、`apple-airplay`、`web-ui` 等目录不要删除。
2. 安装包不会携带本机日志和 WebView 缓存；默认配置只会导入自动回复配置和弹幕区域配置，不会带历史日志。
3. 如果弹幕识别不到，请先确认视频号直播助手的“互动消息”悬浮窗已打开并可见。
4. 如果需要虚拟摄像头，请以管理员身份运行安装相关组件。
